rules_version = '2';

// File uploads/downloads go through the Express backend using the Firebase
// Admin SDK, which bypasses these rules entirely. This denies any direct
// access from a client SDK as a safety net.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
