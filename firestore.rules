rules_version = '2';

// All reads/writes go through the Express backend using the Firebase Admin
// SDK, which bypasses these rules entirely. This denies any direct access
// from a client SDK as a safety net.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
